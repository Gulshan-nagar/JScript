import { ExpressionEvaluator } from "./EvaluateEx.js";

export function handleFunction(functionLines, variables) {
    let line1 = functionLines[0].split(" ");
    console.log(line1);
    console.log(variables);
}